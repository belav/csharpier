enum AnEnum
{
    One,
    Two,
    Three,
}

public enum E
{
    A,
    B = A,
    C = 2 + A,
}

public enum EnumWithDirective
{
    A,
    B = A,
    C = 2 + A
#if DEBUG
    ,
    D = 4,
#endif
}

public enum EnumWithDirective2
{
    A,
    B = A,
    C = 2 + A,
#if DEBUG
    D = 4,
#endif
    E = 5,
}
