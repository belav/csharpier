class ClassName
{
    static Complex operator +(Complex a, Complex b)
    {
        return a.Add(b);
    }

    static Complex operator -(
        Complex someOverlyLongNameToForceBreak,
        Complex someOverlyLongNameToForceBreak2
    )
    {
        return a.Add(b);
    }

    static Complex operator checked -(Complex value);

    static BigInteger IUnaryNegationOperators<BigInteger, BigInteger>.operator checked -(
        BigInteger value
    ) => -value;
}
