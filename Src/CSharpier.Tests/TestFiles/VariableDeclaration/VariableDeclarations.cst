class ClassName
{
    void MethodName()
    {
        int x = 0, y = 1, z = 12;

        string one = "one", two = "two";

        // leading comment
        var x = 0;

        var y = 0; // trailing comment

        // leading comment
        bool b1;

        bool b2; // trailing comment
    }
}